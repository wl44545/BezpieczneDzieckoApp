<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bezpiecznedziecko">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BezpieczneDziecko"
        android:usesCleartextTraffic="true">
        <activity android:name=".parent.children.schedules.parentScheduleDelete"></activity>
        <activity android:name=".parent.children.schedules.parentScheduleEdit" />
        <activity android:name=".parent.children.schedules.parentScheduleAdd" />
        <activity android:name=".parent.children.parentChildDelete" />
        <activity android:name=".parent.children.parentChildEdit" />
        <activity android:name=".authorization.childLogout" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".child.main.childMain" />
        <activity android:name=".parent.maps.parentMap" />
        <activity android:name=".child.maps.childMap" />
        <activity android:name=".parent.main.parentMain" />
        <activity android:name=".authorization.parentLogin" />
        <activity android:name=".authorization.childLogin" />
        <activity android:name=".authorization.childRegister" />
        <activity android:name=".authorization.parentRegister" />
        <activity android:name=".child.schedules.childSchedulesList" />
        <activity android:name=".parent.children.parentChildrenList" />
        <activity android:name=".parent.children.schedules.parentSchedulesList" />
        <activity android:name=".parent.children.schedules.parentSchedule" />
        <activity android:name=".child.schedules.childSchedule" />
        <activity android:name=".parent.children.parentChildProfile" />
        <activity android:name=".parent.main.parentEdit" />
        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".child.main.childLocationService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="location" />
    </application>

</manifest>